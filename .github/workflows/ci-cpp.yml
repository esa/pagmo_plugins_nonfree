name: C++ CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  debug_unix:
    name: Debug ${{ matrix.label }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: linux-x64
            runner: ubuntu-22.04
            conda_pkgs: c-compiler cxx-compiler cmake ninja pagmo-devel libboost-devel>=1.86 tbb tbb-devel eigen nlopt ipopt
          - label: linux-arm64
            runner: ubuntu-22.04-arm
            conda_pkgs: c-compiler cxx-compiler cmake ninja pagmo-devel libboost-devel>=1.86 tbb tbb-devel eigen
          - label: macos-arm64
            runner: macos-latest
            conda_pkgs: c-compiler cxx-compiler libcxx cmake ninja pagmo-devel libboost-devel>=1.86 tbb tbb-devel eigen nlopt ipopt
    steps:
      - uses: actions/checkout@v4
      - uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          channels: conda-forge
          channel-priority: strict
      - name: Install dependencies
        shell: bash
        run: |
          set -e
          conda create -y -q -p "$HOME/local" ${{ matrix.conda_pkgs }}
      - name: Configure
        shell: bash
        run: |
          set -e
          conda run -p "$HOME/local" cmake -S . -B build -G Ninja \
            -DCMAKE_PREFIX_PATH="$HOME/local" \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPPNF_BUILD_TESTS=ON \
            -DPPNF_BUILD_CPP=ON \
            -DPPNF_BUILD_PYTHON=OFF
      - name: Build
        shell: bash
        run: |
          set -e
          conda run -p "$HOME/local" cmake --build build --parallel
      - name: Test
        shell: bash
        run: |
          set -e
          conda run -p "$HOME/local" ctest --test-dir build -VV --output-on-failure -j4

  release_windows_x64:
    name: Release windows-x64
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          channels: conda-forge
          channel-priority: strict
      - name: Configure
        shell: powershell
        run: |
          conda create -y -q -p $env:USERPROFILE\local cmake ninja eigen nlopt ipopt boost-cpp tbb tbb-devel
          conda run -p $env:USERPROFILE\local cmake -S . -B build -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_PREFIX_PATH=$env:USERPROFILE\local `
            -DCMAKE_INSTALL_PREFIX=$env:USERPROFILE\local `
            -DPPNF_BUILD_TESTS=ON `
            -DPPNF_BUILD_CPP=ON `
            -DPPNF_BUILD_PYTHON=OFF
      - name: Build
        shell: powershell
        run: conda run -p $env:USERPROFILE\local cmake --build build --config Release --parallel
      - name: Test
        shell: powershell
        run: |
          $env:PATH = "$PWD\build\Release;$env:USERPROFILE\local\Library\bin;$env:USERPROFILE\local\bin;$env:PATH"
          ctest --test-dir build -VV --output-on-failure -j4 -C Release
